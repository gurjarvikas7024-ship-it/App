name: Build Android APK & AAB Bundle

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

permissions:
  contents: write
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.10.2'
        cache-disabled: true

    - name: Generate Compatible Wrapper
      run: |
        gradle wrapper --gradle-version 8.10.2
        chmod +x ./gradlew

    - name: Build Debug APK and AAB Bundle
      run: ./gradlew assembleDebug bundleDebug --no-daemon

    - name: Upload APK
      uses: actions/upload-artifact@v4
      continue-on-error: true
      with:
        name: app-debug-apk
        path: app/build/outputs/apk/debug/*.apk
        retention-days: 7

    - name: Upload AAB Bundle
      uses: actions/upload-artifact@v4
      continue-on-error: true
      with:
        name: app-debug-aab
        path: app/build/outputs/bundle/debug/*.aab
        retention-days: 7

    - name: Release Direct APK and AAB
      uses: softprops/action-gh-release@v2
      if: github.ref == 'refs/heads/main'
      continue-on-error: true
      with:
        tag_name: v1.0-latest
        name: "Memory Plus App (APK & AAB Bundle)"
        body: "Download the direct APK file for your phone or the AAB bundle below."
        files: |
          app/build/outputs/apk/debug/*.apk
          app/build/outputs/bundle/debug/*.aab
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
